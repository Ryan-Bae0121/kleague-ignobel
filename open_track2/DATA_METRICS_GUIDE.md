# K리그 데이터 지표 완전 가이드

## 📋 목차

1. [원본 데이터 지표](#1-원본-데이터-지표)
2. [경기 정보 지표](#2-경기-정보-지표)
3. [파생 지표 (계산된 지표)](#3-파생-지표-계산된-지표)
4. [Zone 기반 지표](#4-zone-기반-지표)
5. [팀별 경기별 피처](#5-팀별-경기별-피처)
6. [매치업 피처](#6-매치업-피처)
7. [지표 활용 가이드](#7-지표-활용-가이드)

---

## 1. 원본 데이터 지표

### 1.1 raw_data.csv (이벤트 데이터)

#### 기본 식별자
| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| `game_id` | int | 경기 고유 ID | 126283 |
| `action_id` | int | 이벤트 순서 ID | 0, 1, 2, ... |
| `period_id` | int | 하프 구분 (1=전반, 2=후반) | 1, 2 |
| `time_seconds` | float | 경기 시작 후 경과 시간 (초) | 1.033, 2.433 |

#### 팀/선수 정보
| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| `team_id` | int | 팀 고유 ID | 2353, 2354 |
| `team_name_ko` | str | 팀명 (한글) | "FC서울", "울산 HD FC" |
| `player_id` | float | 선수 고유 ID | 356625.0 |
| `player_name_ko` | str | 선수명 (한글) | "이영준" |
| `position_name` | str | 해당 이벤트에서의 포지션 | "CF", "CDM", "CM" |
| `main_position` | str | 주 포지션 | "CF", "CM" |

#### 이벤트 정보
| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| `type_name` | str | 이벤트 타입 | "Pass", "Shot", "Carry", "Tackle" 등 |
| `result_name` | str | 이벤트 결과 | "Successful", "Goal", "On Target" 등 |

**이벤트 타입 (type_name) 목록:**
- **공격**: `Pass`, `Cross`, `Shot`, `Shot_Freekick`, `Carry`, `Take-On`
- **수비**: `Tackle`, `Interception`, `Clearance`, `Block`, `Recovery`
- **기타**: `Duel`, `Pass Received`, `Foul`, `Offside` 등

**이벤트 결과 (result_name) 목록:**
- `Successful`: 성공
- `Goal`: 골
- `On Target`: 유효슈팅
- `Off Target`: 빗나감
- `Blocked`: 차단됨
- `Saved`: 세이브
- (빈 값): 결과 없음 (Pass Received 등)

#### 좌표 정보
| 컬럼명 | 타입 | 설명 | 범위 | 설명 |
|--------|------|------|------|------|
| `start_x` | float | 이벤트 시작 X 좌표 | 0-100 | 정규화된 X 좌표 (골라인→골라인) |
| `start_y` | float | 이벤트 시작 Y 좌표 | 0-100 | 정규화된 Y 좌표 (왼쪽→오른쪽) |
| `end_x` | float | 이벤트 종료 X 좌표 | 0-100 | 정규화된 X 좌표 |
| `end_y` | float | 이벤트 종료 Y 좌표 | 0-100 | 정규화된 Y 좌표 |
| `dx` | float | X 방향 이동 거리 | - | end_x - start_x |
| `dy` | float | Y 방향 이동 거리 | - | end_y - start_y |

**좌표 시스템:**
- **X축**: 0 (자신의 골라인) → 100 (상대 골라인)
- **Y축**: 0 (왼쪽 사이드라인) → 100 (오른쪽 사이드라인)
- **정규화**: 실제 피치 크기를 0-100으로 정규화

---

### 1.2 match_info.csv (경기 정보)

| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| `game_id` | int | 경기 고유 ID | 126283 |
| `season_id` | int | 시즌 ID | 3669 |
| `competition_id` | int | 대회 ID | 587 |
| `game_day` | int | 경기일 (라운드) | 1, 2, 3, ... |
| `game_date` | datetime | 경기 날짜/시간 | 2024-03-01 05:00:00 |
| `venue` | str | 경기장 이름 | "문수월드컵경기장" |
| `competition_name` | str | 대회명 | "K League 1" |
| `country_name` | str | 국가 | "KR" |
| `season_name` | str | 시즌명 | "2024" |

#### 홈팀 정보
| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| `home_team_id` | int | 홈팀 ID | 2354 |
| `home_team_name` | str | 홈팀명 (영문) | "Ulsan HD FC" |
| `home_team_name_ko` | str | 홈팀명 (한글) | "울산 HD FC" |
| `home_score` | int | 홈팀 득점 | 1, 2, 0 |

#### 어웨이팀 정보
| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| `away_team_id` | int | 어웨이팀 ID | 4639 |
| `away_team_name` | str | 어웨이팀명 (영문) | "Pohang Steelers" |
| `away_team_name_ko` | str | 어웨이팀명 (한글) | "포항 스틸러스" |
| `away_score` | int | 어웨이팀 득점 | 0, 1, 2 |

---

## 2. 경기 정보 지표

### 2.1 기본 경기 결과

| 지표 | 설명 | 계산 방법 |
|------|------|----------|
| `goals_for` | 팀의 득점 | 홈팀: `home_score`, 어웨이팀: `away_score` |
| `goals_against` | 팀의 실점 | 홈팀: `away_score`, 어웨이팀: `home_score` |
| `goal_difference` | 골득실 | `goals_for - goals_against` |
| `result` | 경기 결과 | "win", "draw", "loss" |
| `is_home` | 홈/어웨이 여부 | `True` (홈) / `False` (어웨이) |

### 2.2 승점 계산

| 결과 | 홈팀 승점 | 어웨이팀 승점 |
|------|----------|--------------|
| 홈팀 승리 | 3 | 0 |
| 무승부 | 1 | 1 |
| 어웨이팀 승리 | 0 | 3 |

---

## 3. 파생 지표 (계산된 지표)

### 3.1 공격 지표

#### 슈팅 관련
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `shots` | 총 슈팅 수 | `type_name == 'Shot'`인 이벤트 수 | 개 |
| `shots_on_target` | 유효슈팅 수 | `result_name == 'Successful'`인 슈팅 수 | 개 |
| `goals` | 골 수 | `result_name == 'Goal'`인 슈팅 수 | 개 |
| `shot_accuracy` | 슈팅 정확도 | `shots_on_target / shots * 100` | % |
| `conversion_rate` | 골 전환율 | `goals / shots * 100` | % |
| `on_target_rate` | 유효슈팅 비율 | `shots_on_target / shots * 100` | % |

#### 박스 진입 관련
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `box_entries` | 박스 진입 수 | 박스 밖 → 박스 안으로 성공한 패스/캐리 | 개 |
| **박스 좌표** | - | X ≥ 83.3, Y: 21.1 ~ 78.9 | - |

**계산 로직:**
```python
# 박스 진입: 박스 밖에서 박스 안으로 성공한 패스/캐리
box_entries = len(passes_carries[
    (start_x < 83.3) & (end_x >= 83.3) &
    (end_y >= 21.1) & (end_y <= 78.9) &
    (result_name == 'Successful')
])
```

#### 최종 1/3 진입
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `final_third_entries` | 최종 1/3 진입 수 | X < 66.7 → X ≥ 66.7로 성공한 패스/캐리 | 개 |

**계산 로직:**
```python
# 최종 1/3 진입: X < 66.7에서 X >= 66.7로 성공한 패스/캐리
final_third_entries = len(passes_carries[
    (start_x < 66.7) & (end_x >= 66.7) &
    (result_name == 'Successful')
])
```

#### 크로스 관련
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `crosses_attempted` | 크로스 시도 수 | `type_name == 'Cross'`인 이벤트 수 | 개 |
| `crosses_successful` | 성공한 크로스 수 | 크로스 중 `result_name == 'Successful'` | 개 |
| `crosses_success_rate` | 크로스 성공률 | `crosses_successful / crosses_attempted * 100` | % |

#### 전진 패스 (Progressive Pass)
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `progressive_passes_success` | 성공한 전진 패스 수 | 전진 거리 10 이상 또는 최종 1/3 진입 | 개 |

**계산 로직:**
```python
# 전진 패스: 전진 거리 10 이상 또는 최종 1/3 진입
progressive_passes = passes_carries[
    ((end_x - start_x >= 10) | 
     ((start_x < 66.7) & (end_x >= 66.7))) &
    (result_name == 'Successful')
]
```

---

### 3.2 빌드업 지표

#### 패스 관련
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `passes_attempted` | 패스 시도 수 | `type_name == 'Pass'`인 이벤트 수 | 개 |
| `passes_successful` | 성공한 패스 수 | 패스 중 `result_name == 'Successful'` | 개 |
| `pass_success_rate` | 패스 성공률 | `passes_successful / passes_attempted * 100` | % |

#### 롱 패스
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `long_pass_attempted` | 롱 패스 시도 수 | 거리 25 이상인 패스 | 개 |

**계산 로직:**
```python
# 롱 패스: 거리 25 이상
distance = sqrt((end_x - start_x)^2 + (end_y - start_y)^2)
long_passes = passes[distance >= 25]
```

---

### 3.3 수비 지표

| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `recoveries` | 볼 회수 수 | `type_name == 'Recovery'`인 이벤트 수 | 개 |
| `interceptions` | 인터셉트 수 | `type_name == 'Interception'`인 이벤트 수 | 개 |
| `tackles` | 태클 수 | `type_name == 'Tackle'`인 이벤트 수 | 개 |
| `blocks` | 차단 수 | `type_name == 'Block'`인 이벤트 수 | 개 |
| `clearances` | 클리어런스 수 | `type_name == 'Clearance'`인 이벤트 수 | 개 |

#### 듀얼 관련
| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `duels_attempted` | 듀얼 시도 수 | `type_name == 'Duel'`인 이벤트 수 | 개 |
| `duels_successful` | 성공한 듀얼 수 | 듀얼 중 `result_name == 'Successful'` | 개 |
| `duels_success_rate` | 듀얼 성공률 | `duels_successful / duels_attempted * 100` | % |

---

### 3.4 리스크 지표

| 지표명 | 설명 | 계산 방법 | 단위 |
|--------|------|----------|------|
| `defensive_third_turnovers` | 수비 1/3 턴오버 | 수비 1/3에서 실패한 패스/캐리 | 개 |

**계산 로직:**
```python
# 수비 1/3 턴오버: 수비 1/3에서 실패한 패스/캐리
defensive_third_turnovers = len(events[
    (start_x < 33.3) &
    (type_name.isin(['Pass', 'Carry'])) &
    (result_name != 'Successful')
])
```

**주의사항:**
- 이 지표는 **낮을수록 좋음** (리스크 지표)
- 높을수록 수비 1/3에서 실수를 많이 한다는 의미

---

## 4. Zone 기반 지표

### 4.1 Zone 정의

#### X축 Zone (전후 방향)
| Zone | 범위 | 설명 |
|------|------|------|
| **D** (Defense) | 0 ~ 26.25 | 수비 지역 |
| **DM** (Defensive Mid) | 26.25 ~ 52.5 | 수비 미드필드 |
| **AM** (Attacking Mid) | 52.5 ~ 78.75 | 공격 미드필드 |
| **A** (Attack) | 78.75 ~ 100 | 공격 지역 |

#### Y축 Zone (좌우 방향)
| Zone | 범위 | 설명 |
|------|------|------|
| **L** (Left) | 0 ~ 22.67 | 왼쪽 사이드 |
| **C** (Center) | 22.67 ~ 45.33 | 중앙 |
| **R** (Right) | 45.33 ~ 68 | 오른쪽 사이드 |

#### 결합 Zone (12개)
| Zone | 설명 |
|------|------|
| `D-L`, `D-C`, `D-R` | 수비 지역 (좌/중/우) |
| `DM-L`, `DM-C`, `DM-R` | 수비 미드필드 (좌/중/우) |
| `AM-L`, `AM-C`, `AM-R` | 공격 미드필드 (좌/중/우) |
| `A-L`, `A-C`, `A-R` | 공격 지역 (좌/중/우) |

### 4.2 Zone별 지표

#### Zone별 성공률
| 지표명 | 설명 | 계산 방법 |
|--------|------|----------|
| `zone_pass_success_rate` | Zone별 패스 성공률 | 각 Zone에서의 패스 성공률 |
| `zone_shot_success_rate` | Zone별 슛 성공률 | 각 Zone에서의 슛 성공률 |
| `zone_event_count` | Zone별 이벤트 수 | 각 Zone에서 발생한 이벤트 수 |
| `zone_event_pct` | Zone별 이벤트 비율 | 각 Zone의 이벤트 비율 (%) |

#### Zone 프로필
| 지표명 | 설명 | 계산 방법 |
|--------|------|----------|
| `team_zone_profile` | 팀의 Zone 사용 패턴 | 각 Zone에서의 이벤트 분포 |
| `zone_heatmap` | Zone별 이벤트 밀도 | KDE 기반 히트맵 |

---

## 5. 팀별 경기별 피처

### 5.1 기본 정보

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `game_id` | int | 경기 ID |
| `team` | str | 팀명 (한글) |
| `opponent` | str | 상대팀명 (한글) |
| `is_home` | bool | 홈/어웨이 여부 |
| `goals_for` | int | 득점 |
| `goals_against` | int | 실점 |
| `result` | str | 경기 결과 ("win", "draw", "loss") |

### 5.2 공격 피처

| 피처명 | 설명 | 단위 | 범위 |
|--------|------|------|------|
| `shots` | 총 슈팅 수 | 개 | 0 ~ 30+ |
| `shots_on_target` | 유효슈팅 수 | 개 | 0 ~ 15+ |
| `goals` | 골 수 | 개 | 0 ~ 10+ |
| `box_entries` | 박스 진입 수 | 개 | 0 ~ 30+ |
| `final_third_entries` | 최종 1/3 진입 수 | 개 | 0 ~ 100+ |
| `crosses_attempted` | 크로스 시도 수 | 개 | 0 ~ 30+ |
| `crosses_success_rate` | 크로스 성공률 | 비율 | 0.0 ~ 1.0 |
| `progressive_passes_success` | 성공한 전진 패스 수 | 개 | 0 ~ 150+ |

### 5.3 빌드업 피처

| 피처명 | 설명 | 단위 | 범위 |
|--------|------|------|------|
| `passes_attempted` | 패스 시도 수 | 개 | 0 ~ 800+ |
| `pass_success_rate` | 패스 성공률 | 비율 | 0.0 ~ 1.0 |
| `long_pass_attempted` | 롱 패스 시도 수 | 개 | 0 ~ 150+ |

### 5.4 수비 피처

| 피처명 | 설명 | 단위 | 범위 |
|--------|------|------|------|
| `recoveries` | 볼 회수 수 | 개 | 0 ~ 150+ |
| `interceptions` | 인터셉트 수 | 개 | 0 ~ 80+ |
| `tackles` | 태클 수 | 개 | 0 ~ 50+ |
| `duels_success_rate` | 듀얼 성공률 | 비율 | 0.0 ~ 1.0 |

### 5.5 리스크 피처

| 피처명 | 설명 | 단위 | 범위 | 주의 |
|--------|------|------|------|------|
| `defensive_third_turnovers` | 수비 1/3 턴오버 | 개 | 0 ~ 30+ | **낮을수록 좋음** |

---

## 6. 매치업 피처

### 6.1 기본 정보

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `game_id` | int | 경기 ID |
| `team_a` | str | 팀 A (홈팀) |
| `team_b` | str | 팀 B (어웨이팀) |
| `is_home` | bool | 팀 A가 홈인지 여부 |
| `actual_result` | str | 실제 결과 ("win", "draw", "loss") |

### 6.2 평균 피처 (최근 N경기)

#### 팀 A 평균
| 피처명 | 설명 | 예시 |
|--------|------|------|
| `shots_avg` | 팀 A의 최근 N경기 평균 슈팅 수 | 12.5 |
| `box_entries_avg` | 팀 A의 최근 N경기 평균 박스 진입 | 8.3 |
| `pass_success_rate_avg` | 팀 A의 최근 N경기 평균 패스 성공률 | 0.87 |
| ... | (모든 피처에 대해 `_avg` 접미사) | ... |

#### 팀 B 평균
| 피처명 | 설명 | 예시 |
|--------|------|------|
| `shots_avg` (팀 B) | 팀 B의 최근 N경기 평균 슈팅 수 | 11.2 |
| ... | (모든 피처에 대해 `_avg` 접미사) | ... |

#### 팀 B 허용 평균
| 피처명 | 설명 | 예시 |
|--------|------|------|
| `shots_conceded_avg` | 팀 B를 상대한 팀들의 평균 슈팅 수 | 13.8 |
| `box_entries_conceded_avg` | 팀 B를 상대한 팀들의 평균 박스 진입 | 9.5 |
| ... | (모든 피처에 대해 `_conceded_avg` 접미사) | ... |

### 6.3 차이 피처 (diff_*)

| 피처명 | 설명 | 계산 방법 | 의미 |
|--------|------|----------|------|
| `diff_shots` | 슈팅 차이 | `team_a_avg - team_b_conceded_avg` | 양수 = 팀 A 유리 |
| `diff_shots_on_target` | 유효슈팅 차이 | `team_a_avg - team_b_conceded_avg` | 양수 = 팀 A 유리 |
| `diff_box_entries` | 박스 진입 차이 | `team_a_avg - team_b_conceded_avg` | 양수 = 팀 A 유리 |
| `diff_final_third_entries` | 최종 1/3 진입 차이 | `team_a_avg - team_b_conceded_avg` | 양수 = 팀 A 유리 |
| `diff_progressive_passes_success` | 전진 패스 차이 | `team_a_avg - team_b_conceded_avg` | 양수 = 팀 A 유리 |
| `diff_pass_success_rate` | 패스 성공률 차이 | `team_a_avg - team_b_conceded_avg` | 양수 = 팀 A 유리 |
| `diff_tackles` | 태클 차이 | `team_a_avg - team_b_conceded_avg` | 양수 = 팀 A 유리 |
| `diff_defensive_third_turnovers` | 턴오버 차이 | `team_b_conceded_avg - team_a_avg` | **양수 = 팀 A 유리** (역방향) |

**주의사항:**
- 대부분의 지표는 `team_a_avg - team_b_conceded_avg` (양수 = 유리)
- `defensive_third_turnovers`는 역방향: `team_b_conceded_avg - team_a_avg` (낮을수록 좋은 지표이므로)

---

## 7. 지표 활용 가이드

### 7.1 공격 분석 시 사용 지표

**핵심 지표:**
- `box_entries`: 박스 진입이 많을수록 골 기회 증가
- `shots_on_target`: 유효슈팅이 많을수록 골 가능성 증가
- `progressive_passes_success`: 전진 패스가 많을수록 공격력 향상

**보조 지표:**
- `final_third_entries`: 최종 1/3 진입은 공격 기회의 전제
- `crosses_success_rate`: 크로스 성공률은 측면 공격의 효율성
- `pass_success_rate`: 패스 성공률은 빌드업의 안정성

### 7.2 수비 분석 시 사용 지표

**핵심 지표:**
- `tackles`: 태클이 많을수록 적극적 수비
- `interceptions`: 인터셉트가 많을수록 예측 수비
- `recoveries`: 볼 회수가 많을수록 수비 후 전환 능력

**리스크 지표:**
- `defensive_third_turnovers`: **낮을수록 좋음**, 수비 안정성

### 7.3 승리확률 예측 시 중요 피처

**상위 중요 피처 (LightGBM 기준):**
1. `diff_tackles`: 태클 차이
2. `diff_crosses_success_rate`: 크로스 성공률 차이
3. `diff_recoveries`: 볼 회수 차이
4. `diff_interceptions`: 인터셉트 차이
5. `diff_crosses_attempted`: 크로스 시도 차이

### 7.4 전술 레버 시뮬레이션 시 사용 지표

**효과가 큰 전술 레버:**
- `box_entries`: +5 ~ +15 증가 시 승리확률 상승
- `shots_on_target`: +2 ~ +6 증가 시 승리확률 상승
- `progressive_passes_success`: +10 ~ +30 증가 시 승리확률 상승
- `defensive_third_turnovers`: -5 ~ -15 감소 시 승리확률 상승

### 7.5 Zone 기반 분석 시 사용 지표

**공격 관점:**
- `zone_pass_success_rate`: 각 Zone에서의 패스 성공률
- `zone_shot_success_rate`: 각 Zone에서의 슛 성공률
- `team_zone_profile`: 팀의 Zone 사용 패턴

**수비 관점:**
- 상대팀의 Zone별 성공률: 어느 Zone에서 상대가 성공하는가?
- 수비 약점 Zone 식별: 성공률이 높은 Zone = 약점

---

## 8. 지표 계산 공식 요약

### 8.1 박스 진입
```python
box_entries = len(passes_carries[
    (start_x < 83.3) & (end_x >= 83.3) &
    (end_y >= 21.1) & (end_y <= 78.9) &
    (result_name == 'Successful')
])
```

### 8.2 최종 1/3 진입
```python
final_third_entries = len(passes_carries[
    (start_x < 66.7) & (end_x >= 66.7) &
    (result_name == 'Successful')
])
```

### 8.3 전진 패스
```python
progressive_passes = passes_carries[
    ((end_x - start_x >= 10) | 
     ((start_x < 66.7) & (end_x >= 66.7))) &
    (result_name == 'Successful')
]
```

### 8.4 롱 패스
```python
distance = sqrt((end_x - start_x)^2 + (end_y - start_y)^2)
long_passes = passes[distance >= 25]
```

### 8.5 수비 1/3 턴오버
```python
defensive_third_turnovers = len(events[
    (start_x < 33.3) &
    (type_name.isin(['Pass', 'Carry'])) &
    (result_name != 'Successful')
])
```

### 8.6 성공률 계산
```python
success_rate = (successful_events / total_events) * 100
```

---

## 9. 지표 해석 가이드

### 9.1 높을수록 좋은 지표 (공격/수비 능력)

- ✅ `shots`, `shots_on_target`, `goals`
- ✅ `box_entries`, `final_third_entries`
- ✅ `progressive_passes_success`
- ✅ `pass_success_rate`, `crosses_success_rate`
- ✅ `recoveries`, `interceptions`, `tackles`
- ✅ `duels_success_rate`

### 9.2 낮을수록 좋은 지표 (리스크)

- ⚠️ `defensive_third_turnovers`: 수비 1/3에서 실수

### 9.3 비교 기준

**리그 평균 대비:**
- `team_avg - league_avg > 0`: 리그 평균보다 우수
- `team_avg - league_avg < 0`: 리그 평균보다 부족

**상대팀 대비:**
- `team_avg - opponent_conceded_avg > 0`: 상대팀 약점을 노릴 수 있음
- `team_avg - opponent_conceded_avg < 0`: 상대팀이 강한 부분

---

## 10. 데이터 품질 체크리스트

### 10.1 기본 검증
- [ ] 좌표 범위: 0 ≤ x, y ≤ 100
- [ ] 이벤트 타입: 알려진 타입만 존재
- [ ] 결과 타입: 알려진 결과만 존재
- [ ] 경기 ID: 모든 이벤트에 game_id 존재

### 10.2 논리적 검증
- [ ] 골 수 ≤ 슈팅 수
- [ ] 유효슈팅 수 ≤ 슈팅 수
- [ ] 성공한 패스 수 ≤ 패스 시도 수
- [ ] 박스 진입 수 ≤ 최종 1/3 진입 수

### 10.3 통계적 검증
- [ ] 이상치 확인 (너무 큰/작은 값)
- [ ] 결측치 확인
- [ ] 분포 확인 (정상 분포인지)

---

## 📚 참고 자료

- **원본 데이터**: `data/raw/raw_data.csv`, `data/raw/match_info.csv`
- **처리된 데이터**: `data/processed/team_match_features.csv`, `data/processed/matchup_features.csv`
- **노트북**: `notebooks/06_win_probability_simulator.ipynb` (피처 계산 로직)

---

## 🔄 업데이트 이력

- 2024-01: 초기 버전 작성
- 모든 지표 정의 및 계산 방법 정리

